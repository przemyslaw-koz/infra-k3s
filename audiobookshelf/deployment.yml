apiVersion: apps/v1
kind: Deployment
metadata:
  name: audiobookshelf
  labels:
    app: audiobookshelf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audiobookshelf
  template:
    metadata:
      labels:
        app: audiobookshelf
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
      containers:
        - name: audiobookshelf
          image: ghcr.io/advplyr/audiobookshelf:latest
          env:
            - name: TZ
              value: Europe/Warsaw
          ports:
            - containerPort: 80
              name: http
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - mountPath: /config
              name: config
              readOnly: false
            - mountPath: /metadata
              name: metadata
              readOnly: false
            - mountPath: /audiobooks
              name: audiobooks
              readOnly: true
      volumes:
        - name: config
          hostPath:
            path: /srv/audiobookshelf/config
            type: DirectoryOrCreate
        - name: metadata
          hostPath:
            path: /srv/audiobookshelf/metadata
            type: DirectoryOrCreate
        - name: audiobooks
          hostPath:
            path: /srv/audiobookshelf/audiobooks
            type: DirectoryOrCreate
