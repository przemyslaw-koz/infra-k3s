---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: linkding
spec:
  storageClassName: local-srv-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
