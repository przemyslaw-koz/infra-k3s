---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config
spec:
  storageClassName: local-srv-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage:

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metadata
spec:
  storageClassName: local-srv-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage:

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audiobooks
spec:
  storageClassName: local-srv-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage:
